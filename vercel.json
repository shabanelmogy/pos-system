{
  "installCommand": "cd pos-frontend && npm install",
  "buildCommand": "cd pos-frontend && npm run build",
  "outputDirectory": "pos-frontend/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
